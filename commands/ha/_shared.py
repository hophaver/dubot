HA_MAPPINGS_FILE = "data/ha_mappings.json"
"""Friendly name -> entity_id mappings (from /explain)."""

HA_ENTITIES_ALLOWLIST_FILE = "data/ha_entities_allowlist.json"
"""Optional. List of entity_ids the bot can see/control. If missing or empty, all entities (from HA token) are used."""
